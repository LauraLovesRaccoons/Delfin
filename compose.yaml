version: "3.1"
services:
  db:
    image: mysql
    container_name: db
    restart: always
    environment:
      MYSQL_DATABASE: delfin_db
      MYSQL_ROOT_PASSWORD: root #! EXPOSED
    ports:
      - "3308:3306"
  phpmyadmin:
    image: phpmyadmin:latest
    #image: arm64v8/phpmyadmin:latest
    restart: always
    environment:
      PMA_HOST: db
      PMA_USER: root      #! EXPOSED
      PMA_PASSWORD: root  #! EXPOSED
    ports:
      - "8080:80"
  #php-apache-environment:
    #container_name: php-apache
    #environment:
      #- max_upload=100M
    #build:
      #context: ./php
      #dockerfile: dockerfile
    #volumes:
      #- ./php/src:/var/www/html/ #Path to folder in windows : Path to folder in Linux
    #ports:
      #- 8000-8010:8000-8010
  #wordpress:
    #image: wordpress:latest
    #restart: always
    #ports:
      #- 80:80
    #environment:
      #WORDPRESS_DB_HOST: db
      #WORDPRESS_DB_USER: root
      #WORDPRESS_DB_PASSWORD: root
      #WORDPRESS_DB_NAME: wordpress
    #volumes:
      #- wordpress:/var/www/html
#volumes:
  #db:
  #wordpress:
