console.log("footer js loads");

// alert("footer's js loads");

// redirect if someone presses the bowser's forward or backward button

// don't use js , because if someone uses the browser's back or forward button, the js will not be executed

